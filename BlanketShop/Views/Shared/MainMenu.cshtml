@model List<Model.EF.Menu>

<div class="menu">
    <ul>
        @foreach (var item in Model)
        {
            <li class="active"><a href="@item.Link" target="@item.Target" style="background:#053d1e;font-family:sans-serif;">@item.Text</a></li>
        }
        <div class="clear"></div>
    </ul>
</div>
