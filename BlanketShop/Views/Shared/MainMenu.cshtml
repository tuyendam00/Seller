@model List<Model.EF.Menu>

<div class="menu">
    <ul>
        @foreach (var item in Model)
        {
            <li class="active"  style="font-family:sans-serif"><a href="@item.Link" target="@item.Target">@item.Text</a></li>
        }
        <div class="clear"></div>
    </ul>
</div>
