@model IEnumerable<Model.EF.Materials>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layouts.cshtml";
}

@section header{
    Danh sách chất liệu
}

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Material_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Material_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Material_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Material_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Material_id })
        </td>
    </tr>
}

</table>
